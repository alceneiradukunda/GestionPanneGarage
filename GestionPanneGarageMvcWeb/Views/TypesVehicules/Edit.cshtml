@model GestionPanneGarageMvcWeb.Models.TypesVehicule
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Layouts/_Default.cshtml";
}
@Html.Raw(TempData["msg"])
<div  style="text-align:center;">
<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main" style="background-color:rgb(0, 255, 255)">
<h3><i><b>Mis a jour sur TypesVehicule</b></i></h3>
@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend><u><i><b>Liste sur TypesVehicule</b></i></u></legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.DescriptionsVehicules)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DescriptionsVehicules)
            @Html.ValidationMessageFor(model => model.DescriptionsVehicules)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
</div>
    </div>
