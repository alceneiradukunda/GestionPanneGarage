@model GestionPanneGarageMvcWeb.Models.CategoriesArticle

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Layout/_Default1.cshtml";
}
@Html.Raw(TempData["msg"])
<div  style="text-align:center;">
<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main" style="background-color:rgb(0, 255, 255)">
<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
         <legend><u><i><b>Liste sur CategoriesArticle</b></i></u></legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.NomCategorieArticle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NomCategorieArticle)
            @Html.ValidationMessageFor(model => model.NomCategorieArticle)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index1")
</div>
</div>
</div>